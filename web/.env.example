# Onde o BFF acha o núcleo. NUNCA exposto ao browser.
CORE_URL=http://localhost:8000
# Timeout da chamada ao núcleo, em ms. Estourou -> fallback local por regras.
CORE_TIMEOUT_MS=2500
# Base de conhecimento para o fallback (mesmo arquivo do núcleo).
FLOWS_PATH=../core/data/flows.json
